{
  "$defs": {
    "ApprovalStatus": {
      "enum": [
        "approved",
        "rejected"
      ],
      "title": "ApprovalStatus",
      "type": "string"
    },
    "PendingVisibilityApproval": {
      "additionalProperties": false,
      "description": "One approval, for one field of one metric.",
      "properties": {
        "approval_status": {
          "$ref": "#/$defs/ApprovalStatus"
        },
        "approved_at": {
          "format": "date",
          "title": "Approved At",
          "type": "string"
        },
        "approved_by_role": {
          "description": "A role from the ownership registry. Never an individual.",
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9_]*$",
          "title": "Approved By Role",
          "type": "string"
        },
        "expires_at": {
          "anyOf": [
            {
              "format": "date",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Expires At"
        },
        "field_name": {
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9_]*$",
          "title": "Field Name",
          "type": "string"
        },
        "metric_id": {
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9_]*$",
          "title": "Metric Id",
          "type": "string"
        },
        "source_commit": {
          "description": "Binds the approval to approved content.",
          "minLength": 7,
          "title": "Source Commit",
          "type": "string"
        }
      },
      "required": [
        "metric_id",
        "field_name",
        "approval_status",
        "approved_by_role",
        "approved_at",
        "source_commit"
      ],
      "title": "PendingVisibilityApproval",
      "type": "object"
    }
  },
  "$id": "https://intelligence-agent/semantic-catalog/pending_visibility_approvals.schema.json",
  "additionalProperties": false,
  "description": "``semantic/governance/pending-visibility-approvals.yaml``.",
  "properties": {
    "approvals": {
      "default": [],
      "items": {
        "$ref": "#/$defs/PendingVisibilityApproval"
      },
      "title": "Approvals",
      "type": "array"
    },
    "catalog_schema_version": {
      "title": "Catalog Schema Version",
      "type": "integer"
    },
    "kind": {
      "const": "pending_visibility_approvals",
      "title": "Kind",
      "type": "string"
    }
  },
  "required": [
    "catalog_schema_version",
    "kind"
  ],
  "title": "PendingVisibilityApprovals",
  "type": "object",
  "x-catalog-schema-version": 3,
  "x-do-not-edit": "Generated from the Pydantic models. Edit the models, then regenerate. This file is never a source of truth.",
  "x-generated-by": "semantic_catalog.contracts.export",
  "x-supported-schema-versions": [
    3,
    2,
    1
  ]
}
