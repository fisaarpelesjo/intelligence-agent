{
  "$defs": {
    "ComparabilityContent": {
      "additionalProperties": false,
      "properties": {
        "caveat": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Caveat"
        },
        "lang": {
          "const": "pt-BR",
          "description": "Explicit language declaration; never inferred.",
          "title": "Lang",
          "type": "string"
        },
        "reason": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Reason"
        }
      },
      "required": [
        "lang"
      ],
      "title": "ComparabilityContent",
      "type": "object"
    },
    "Relation": {
      "description": "Closed set. Anything not declared is denied by Gate 6.",
      "enum": [
        "combinable",
        "comparable_with_caveat",
        "not_comparable"
      ],
      "title": "Relation",
      "type": "string"
    },
    "RuleSubject": {
      "enum": [
        "metric_pair",
        "source_pair"
      ],
      "title": "RuleSubject",
      "type": "string"
    }
  },
  "$id": "https://intelligence-agent/semantic-catalog/comparability.schema.json",
  "additionalProperties": false,
  "description": "``semantic/comparability/{id}.yaml``.",
  "properties": {
    "catalog_schema_version": {
      "title": "Catalog Schema Version",
      "type": "integer"
    },
    "content": {
      "$ref": "#/$defs/ComparabilityContent"
    },
    "id": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Id",
      "type": "string"
    },
    "kind": {
      "const": "comparability",
      "title": "Kind",
      "type": "string"
    },
    "left": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Left",
      "type": "string"
    },
    "relation": {
      "$ref": "#/$defs/Relation"
    },
    "right": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Right",
      "type": "string"
    },
    "subject": {
      "$ref": "#/$defs/RuleSubject"
    }
  },
  "required": [
    "catalog_schema_version",
    "kind",
    "id",
    "subject",
    "left",
    "right",
    "relation",
    "content"
  ],
  "title": "ComparabilityRule",
  "type": "object",
  "x-catalog-schema-version": 3,
  "x-do-not-edit": "Generated from the Pydantic models. Edit the models, then regenerate. This file is never a source of truth.",
  "x-generated-by": "semantic_catalog.contracts.export",
  "x-supported-schema-versions": [
    3,
    2,
    1
  ]
}
