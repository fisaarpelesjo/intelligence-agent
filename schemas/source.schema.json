{
  "$defs": {
    "Outage": {
      "additionalProperties": false,
      "description": "A window in which the source produced no usable data.",
      "properties": {
        "from": {
          "format": "date",
          "title": "From",
          "type": "string"
        },
        "reason": {
          "minLength": 1,
          "title": "Reason",
          "type": "string"
        },
        "to": {
          "format": "date",
          "title": "To",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to",
        "reason"
      ],
      "title": "Outage",
      "type": "object"
    },
    "Restatement": {
      "additionalProperties": false,
      "description": "The source republished figures for a past window (FR-028).\n\nDistinct from a definition change: the meaning did not move, the numbers did.",
      "properties": {
        "affects_from": {
          "format": "date",
          "title": "Affects From",
          "type": "string"
        },
        "affects_to": {
          "format": "date",
          "title": "Affects To",
          "type": "string"
        },
        "reason": {
          "minLength": 1,
          "title": "Reason",
          "type": "string"
        },
        "restated_at": {
          "format": "date",
          "title": "Restated At",
          "type": "string"
        }
      },
      "required": [
        "restated_at",
        "affects_from",
        "affects_to",
        "reason"
      ],
      "title": "Restatement",
      "type": "object"
    },
    "SourceContent": {
      "additionalProperties": false,
      "description": "pt-BR human-facing content for a source.",
      "properties": {
        "label": {
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "lang": {
          "const": "pt-BR",
          "description": "Explicit language declaration; never inferred.",
          "title": "Lang",
          "type": "string"
        },
        "limitations": {
          "default": [],
          "items": {
            "minLength": 1,
            "type": "string"
          },
          "title": "Limitations",
          "type": "array"
        }
      },
      "required": [
        "lang",
        "label"
      ],
      "title": "SourceContent",
      "type": "object"
    },
    "SourceType": {
      "enum": [
        "product_telemetry",
        "app_store"
      ],
      "title": "SourceType",
      "type": "string"
    }
  },
  "$id": "https://intelligence-agent/semantic-catalog/source.schema.json",
  "additionalProperties": false,
  "description": "``semantic/sources/{id}.yaml``.",
  "properties": {
    "catalog_schema_version": {
      "title": "Catalog Schema Version",
      "type": "integer"
    },
    "content": {
      "$ref": "#/$defs/SourceContent"
    },
    "delay_tolerance": {
      "description": "REQUIRED, no default. Business input D-1; sets how often the system refuses.",
      "format": "duration",
      "title": "Delay Tolerance",
      "type": "string"
    },
    "earliest_available_date": {
      "format": "date",
      "title": "Earliest Available Date",
      "type": "string"
    },
    "expected_refresh_interval": {
      "format": "duration",
      "title": "Expected Refresh Interval",
      "type": "string"
    },
    "id": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Id",
      "type": "string"
    },
    "kind": {
      "const": "source",
      "title": "Kind",
      "type": "string"
    },
    "outages": {
      "default": [],
      "items": {
        "$ref": "#/$defs/Outage"
      },
      "title": "Outages",
      "type": "array"
    },
    "owner": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Owner",
      "type": "string"
    },
    "platform": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Platform",
      "type": "string"
    },
    "product": {
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_]*$",
      "title": "Product",
      "type": "string"
    },
    "reporting_timezone": {
      "description": "Native IANA zone, preserved verbatim and never normalised.",
      "title": "Reporting Timezone",
      "type": "string"
    },
    "restatements": {
      "default": [],
      "items": {
        "$ref": "#/$defs/Restatement"
      },
      "title": "Restatements",
      "type": "array"
    },
    "store": {
      "anyOf": [
        {
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9_]*$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Present only for app-store sources (A-7).",
      "title": "Store"
    },
    "type": {
      "$ref": "#/$defs/SourceType"
    }
  },
  "required": [
    "catalog_schema_version",
    "kind",
    "id",
    "type",
    "product",
    "platform",
    "reporting_timezone",
    "earliest_available_date",
    "expected_refresh_interval",
    "delay_tolerance",
    "owner",
    "content"
  ],
  "title": "Source",
  "type": "object",
  "x-catalog-schema-version": 3,
  "x-do-not-edit": "Generated from the Pydantic models. Edit the models, then regenerate. This file is never a source of truth.",
  "x-generated-by": "semantic_catalog.contracts.export",
  "x-supported-schema-versions": [
    3,
    2,
    1
  ]
}
