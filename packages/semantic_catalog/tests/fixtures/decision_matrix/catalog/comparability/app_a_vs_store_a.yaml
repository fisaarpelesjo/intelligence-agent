# T082 decision-matrix fixture — NOT production data.
#
# Comparability is deny-by-default: a pair with no rule here is refused, and the
# absence of a rule is never read as permission.

catalog_schema_version: 1
kind: comparability

id: app_a_vs_store_a
subject: source_pair
left: app_a
right: store_a
relation: combinable
content:
  lang: pt-BR
  reason: >-
    As duas fontes do fixture emitem a mesma definição de evento, no mesmo fuso
    de relatório, para os mesmos produtos.
