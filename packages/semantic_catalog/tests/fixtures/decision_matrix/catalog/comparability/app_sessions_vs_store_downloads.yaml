# T082 decision-matrix fixture — NOT production data.
#
# Comparability is deny-by-default: a pair with no rule here is refused, and the
# absence of a rule is never read as permission.

# The pair that motivates the gate: near-synonyms in conversation, different
# events in the data.
catalog_schema_version: 1
kind: comparability

id: app_sessions_vs_store_downloads
subject: metric_pair
left: app_sessions
right: store_downloads
relation: not_comparable
content:
  lang: pt-BR
  reason: >-
    Sessões são registradas no dispositivo e downloads são relatados pela loja.
    São eventos diferentes, medidos por sistemas diferentes; a diferença entre
    os dois números não mede desistência.
