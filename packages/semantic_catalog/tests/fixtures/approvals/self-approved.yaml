# T048 unit-test fixture — NOT production data, NOT a production approval.
# The governed production seed is `semantic/governance/pending-visibility-approvals.yaml`
# (T039) and is validated separately. Nothing here grants anything in the real
# catalog.

# Approved by the same role that proposed the change. Refused unless the
# governed policy grants self-approval explicitly — the tests drive both.
catalog_schema_version: 1
kind: pending_visibility_approvals
approvals:
  - metric_id: fixture_metric
    field_name: public_name
    approval_status: approved
    approved_by_role: product_analytics
    approved_at: 2026-08-01
    source_commit: fixture0
