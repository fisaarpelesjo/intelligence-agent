# tests/fixtures/freshness

one fixture per freshness state plus tolerance and coverage edge cases (T063)

**Unit-test fixtures only.** They never represent production data, production approvals or
integration readiness. After EXT-A readiness (T109) is declared, fixture fallback in integration or
release CI fails closed (T101).
