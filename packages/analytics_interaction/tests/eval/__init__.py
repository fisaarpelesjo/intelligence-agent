"""Evaluation harness. **TEST-ONLY, and never evidence for any external record.**

Eight deterministic graders and the regression that runs them. See `graders.py` for the
comparators and `test_grader_regression.py` for what a green run does and does not
establish.
"""
