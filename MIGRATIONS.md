# Migrations

Migrações devem preservar personalizacoes. `scripts/migrate.py` nunca sobrescreve arquivos existentes silenciosamente e deve registrar limitacoes quando uma migracao exigir decisao humana.

