# Architecture Context

Componentes principais:

- documentos canonicos (`ENGINEERING.md`, `AGENTS.md`, specs, planos e tarefas);
- estado versionado em `.project`;
- schemas JSON para YAML operacional;
- scripts Python para verificar, diagnosticar, retomar, reconciliar, migrar e registrar checkpoints;
- perfis de workflow e stack;
- testes unitarios e fixtures.

